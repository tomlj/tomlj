dependencyManagement {
  imports {
    mavenBom 'org.junit:junit-bom:6.1.3'
  }
  dependencies {
    dependency('com.google.errorprone:error_prone_core:2.50.0')
    dependencySet(group: 'org.antlr', version: '4.13.2') {
      entry 'antlr4'
      entry 'antlr4-runtime'
    }
    dependency('org.checkerframework:checker-qual:3.55.1')
    // The official TOML test suite, fetched as a source archive of the tagged release
    dependency('toml-lang:toml-test:2.2.0')
  }
}
